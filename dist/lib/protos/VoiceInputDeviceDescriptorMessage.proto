syntax = "proto2";

import "AudioFormatSettingsMessage.proto";

message VoiceInputDeviceDescriptor {
  optional AudioFormatSettings defaultFormat = 1;
  repeated AudioFormatSettings supportedFormats = 2;
}